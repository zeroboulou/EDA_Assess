#include <iostream>
#include "placement/placement.cpp"
int main()
{
    ReadFile();
    CreatFirstLayout();
    OptimizeLayout();
    for (int i = 0; i < m.Instances; i)
    {
        cout<<"C"<<i 1<<" "<<"("<<layout[i 1].x<<","<<layout[i 1].y<<")"<<endl;
    }

}
